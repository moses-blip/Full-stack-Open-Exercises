```mermaid
sequenceDiagram
    participant U as User
    participant F as SPA Frontend
    participant B as Backend API

    U->>F: 1. Click "New Note"
    U->>F: 2. Enter content & click Save
    F->>B: 3. POST /notes (note data)
    B-->>F: 4. Save in DB & return new note ID
    F-->>U: 5. Update UI (new note appears instantly)
